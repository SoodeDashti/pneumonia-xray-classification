# Notebook Files

- **`pneumonia_cnn_classification_FULL.ipynb`** — the complete notebook:
  dataset loading, all three architectures (simple CNN, custom deep CNN,
  ResNet50 transfer learning) with their full build/train/evaluate code,
  the model comparison, and the uncertainty estimation section. **This is
  the file to review.**

- `pneumonia_cnn_classification.ipynb` — an earlier, partial version kept
  for reference (some early cells were lost across disconnected Colab
  sessions before the full version above was reconstructed).
